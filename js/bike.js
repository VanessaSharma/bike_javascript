function Bike(title, description, image, lastSeen, model) {
  this.title = title;
  this.description = description;
  this.image = image;
  this.lastSeen = lastSeen;
  this.model = model;
}

Bike.prototype.getBike = function(title, description, image, lastSeen, model);
